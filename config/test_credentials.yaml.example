# =============================================================================
# QA Test Credentials — 测试用外部 API Key 配置
# =============================================================================
#
# 使用说明:
#   1. 将本文件复制为 config/test_credentials.yaml（去掉 .example 后缀）
#   2. 填入你的 API Key（替换 <YOUR_KEY_HERE>）
#   3. 该文件已在 .gitignore 中，不会被提交到 Git
#
# 测试脚本会自动读取此文件，在需要切换 Provider 时动态修改 settings.yaml
# 测试完成后自动恢复原始 settings.yaml
# =============================================================================

# DeepSeek LLM (用于 K 系列测试: Provider 切换)
# 申请地址: https://platform.deepseek.com/
deepseek:
  api_key: "<YOUR_DEEPSEEK_API_KEY>"
  model: "deepseek-chat"
  base_url: "https://api.deepseek.com"  # 默认值，通常不需要修改

# 以下为可选项，如果你有其他 Provider 的 Key 也可以填入

# OpenAI 直连 (可选)
# openai:
#   api_key: "<YOUR_OPENAI_API_KEY>"
#   model: "gpt-4o"
